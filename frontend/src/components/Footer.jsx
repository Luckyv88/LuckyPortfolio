import React from 'react';

const Footer = () => (
  <footer className="footer">
    © {new Date().getFullYear()} Lucky Vishwakarma — Built with React & Node. • <a href="/certificates">Certificates</a>
  </footer>
);

export default Footer;
